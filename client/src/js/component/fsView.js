import React from 'react';
const {div} = React.DOM;
import component from 'component';

export default component({
    displayName: 'Shell',

    render() {
        return div({},
            'fsview'
        );
    }
});
