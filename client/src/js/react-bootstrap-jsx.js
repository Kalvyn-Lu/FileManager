import React from 'react';
import {Glyphicon, Input, ProgressBar} from 'react-bootstrap';

export default {
    Glyph: React.createFactory(Glyphicon),
    Input: React.createFactory(Input),
    ProgressBar: React.createFactory(ProgressBar)
};
